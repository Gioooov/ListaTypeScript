// {
//     "compilerOptions": {
//         "target": "ESNext",
//         "module": "ESNext",
//         "moduleResolution": "bundler", // Ou "nodenext"
//         "outDir": "./dist",
//         "rootDir": "./src",
//         "strict": true,
//         "lib": ["ESNext", "DOM"]
//     }
// }

{
<<<<<<< HEAD
  "compilerOptions": {
<<<<<<< Updated upstream
    /* 1. Isolamento de arquivos: Força cada arquivo a ser um mundo próprio, 
       resolvendo o erro de 'variável já declarada' entre arquivos diferentes. */
    //"moduleDetection": "force", 
    
    /* 2. Versão do JS: Define para qual versão do JavaScript o código será traduzido. */
    "target": "es2016",
    
    /* 3. Padrão de Módulos: Usa o CommonJS, que é o padrão nativo do Node.js. */
    "module": "commonjs",
    
    /* 4. Pasta de Entrada: O TS vai procurar seus códigos .ts dentro da pasta /src. 
       Certifique-se de que seus arquivos de exercícios estão lá dentro! */
    "rootDir": "./src",
    
    /* 5. Pasta de Saída: Onde o arquivo .js final será criado. */
    "outDir": "./dist",
    
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true
  },
  /* 6. Inclusão: Diz ao TS para vigiar todos os arquivos .ts dentro da pasta src. */
  //"include": ["src/**/*"],
  
  /* 7. Exclusão: Impede que o TS tente validar arquivos dentro da pasta dist ou node_modules. */
  //"exclude": ["node_modules", "dist"]
=======
    /* 1. O SEGREDO: 'moduleDetection: force' trata cada arquivo como isolado,
       mesmo que você não use a palavra 'export' ou 'import'. 
       Isso mata o erro de "Variável já declarada" no VS Code. */
    "moduleDetection": "force", 
    /* 2. Para rodar no Navegador ou Node moderno, mude o 'module' para 'ESNext' ou 'None'.
       Se você quer apenas que o código rode simples no navegador via script, 
       use "CommonJS" apenas se for rodar exclusivamente no Node. */
    "module": "ESNext", 
    /* 3. Versão do JavaScript de saída. */
    "target": "ES6",
    /* 4. Onde estão seus arquivos .ts originais. */
    "rootDir": "./src",
    /* 5. Onde o TS vai salvar os arquivos .js convertidos. */
    "outDir": "./dist",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

  },
  /* 6. Garante que o TS só olhe para a pasta src e ignore a dist. */
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
>>>>>>> Stashed changes
=======
"compilerOptions": {
"target": "es2016",
"module": "commonjs",
"rootDir": "./src",
"outDir": "./dist",
"esModuleInterop": true,
"forceConsistentCasingInFileNames": true,
"strict": true,
"skipLibCheck": true
}
>>>>>>> parent of cbf627c (Merge branch 'main' of https://github.com/Gioooov/ListaTypeScript)
}